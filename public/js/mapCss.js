$(document).ready(function() {
  // CSSMap;
  $('#map-continents').CSSMap({
    size: 1450,
    tooltips: 'floating-top-center',
    responsive: 'auto',
    tapOnce: true,
    mobileSupport: true
  });
});
